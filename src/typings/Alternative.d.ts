export type Alternative = {
  name: string;
  action: () => void;
  icon?: string;
};

// If there's a binary related to the thing we're calling, pipe in some data from there?
// - Execute the binary?
// - Delete the binary?
// - Show a dialog with like binary Properties?

// If there's a process related to the thing we're calling, pipe in some data from there?
// - Exit the process as a context option?
// - Minimize the window?
// - Maximize the window?
// - Show a dialog with like process Properties?

// Example: Desktop

// Option to
// - cut
// - paste
// - copy
// - delete (move to trash?)
// - make shortcut to

// Option to
// - Create new folder

// I need a clipboard
